# Skills

Reusable skill definitions the assistant can draw on. Skills can be sourced from GitHub repositories (listed in `github.txt`) or defined locally in the `local/` subdirectory.

- `github.txt` — GitHub URLs pointing to skill definitions, one per line
- `local/` — locally defined skills, each in its own subdirectory
