# Tasks

Tracked work items with dependencies. Each `.yml` file defines a task.
The agent planner uses tasks to determine what work is ready (unblocked).
Tasks are synced to Neo4j for graph-powered dependency queries.
