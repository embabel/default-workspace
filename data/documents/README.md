# Documents

Living documents the agent actively maintains. All `.md` files in this directory are loaded as `LivingDocument` instances — stored in the knowledge graph, vector-indexed, and accessible via tools.

The agent can read and update these documents. External edits (direct file changes) are detected and raise events that can trigger actions.

Create any `.md` file here to make it available to the agent.
